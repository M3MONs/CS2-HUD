import { memo } from "react";

export const PlantedBomb = memo(function PlantedBomb() {
    return (
        <div
            className="classic-radar__planted-bomb"
            title="Bomb"
        />
    );
});
